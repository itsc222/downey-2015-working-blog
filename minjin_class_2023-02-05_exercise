def even_sum(n):
    a = n
    b = 0
    while n > 0:
        if n % 2 == 1:
            n = n - 1
        else:
            b = b + n
            n = n - 2
    return f"The sum of all even numbers from 0 to {int(a)} is {int(b)}"

print(even_sum(float(input("Please type in a number: "))))